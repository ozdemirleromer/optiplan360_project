# ============================================================
# OptiPlan360 — Backend Ortam Değişkenleri
# Bu dosyayı kopyalayın: cp .env.example .env
# ============================================================

# ── Güvenlik ─────────────────────────────────────────────────
# JWT imzalama anahtarı — minimum 32 karakter, rastgele üret:
#   python -c "import secrets; print(secrets.token_hex(32))"
OPTIPLAN_SECRET_KEY=buraya-rastgele-uzun-bir-anahtar-girin-min-32-karakter

# Ortam: development | production
OPTIPLAN_ENV=development

# ── Veritabanı ───────────────────────────────────────────────
# SQLite (development): boş bırakın, otomatik kullanılır
# PostgreSQL (production):
# DATABASE_URL=postgresql://user:password@localhost:5432/optiplan360

# ── Orchestrator ─────────────────────────────────────────────
# Service-to-service internal API key
# ORCH_INTERNAL_KEY=internal-service-key-buraya

# Orchestrator Node.js servis URL (yoksa yerel işleme devreye girer)
# ORCH_BASE_URL=http://localhost:3001

# ── OptiPlanning Entegrasyonu ─────────────────────────────────
# CSV/XLSX dosyaları buraya yazılır (OptiPlanning buradan okur)
OPTIPLAN_IMPORT_DIR=C:\Biesse\OptiPlanning\ImpFile

# OptiPlanning'in ürettiği XML çözüm dosyaları (Tmp\Sol dizini)
OPTIPLAN_EXPORT_DIR=C:\Biesse\OptiPlanning\Tmp\Sol

# OptiPlanning.exe tam yolu (otomatik tetikleme)
OPTIPLAN_EXE_PATH=C:\Biesse\OptiPlanning\System\OptiPlanning.exe

# Makine transfer klasörü (inbox/processed/failed alt klasörleri)
MACHINE_DROP_DIR=C:\Biesse\OptiPlanning\Tx

# XML bekleme zaman aşımı (saniye, 20 dakika)
XML_COLLECT_TIMEOUT_S=1200

# Makine ACK zaman aşımı (saniye, 5 dakika)
MACHINE_ACK_TIMEOUT_S=300

# ── AI Servisleri ────────────────────────────────────────────
# OpenAI (price tracking AI extraction için)
# OPENAI_API_KEY=sk-...

# Google Gemini (AI Assistant için)
# GEMINI_API_KEY=...

# ── Frontend URL (CORS) ───────────────────────────────────────
# FRONTEND_URL=http://localhost:3001
